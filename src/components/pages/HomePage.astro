---
import { siteContent } from "../../data/siteContent";

const { home } = siteContent;
const mobileHeroBody =
  "886 Studios backs founders from Taipei with capital, operator support, and a proven Silicon Valley network.";
const photoLoopSets = [false, true, true, true];
const isExternalHeroCta = home.hero.cta.href.startsWith("http");
---

<div class="page" id="page-home">
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-orb orb-1"></div>
      <div class="hero-orb orb-2"></div>
      <div class="hero-orb orb-3"></div>
      <div class="hero-grid"></div>
    </div>
    <div class="hero-inner">
      <h1 class="hero-h1">
        {home.hero.titleLead}<br /><em>{home.hero.titleAccent}</em>
      </h1>
      <p class="hero-sub hero-sub--desktop">
        {home.hero.body.split("\n").map((line, index) => (
          <>
            {index > 0 && <br />}
            <span>{line}</span>
          </>
        ))}
      </p>
      <p class="hero-sub hero-sub--mobile">{mobileHeroBody}</p>
      <div class="hero-actions">
        <a
          class="btn-pri"
          href={home.hero.cta.href}
          target={isExternalHeroCta ? "_blank" : undefined}
          rel={isExternalHeroCta ? "noopener noreferrer" : undefined}
        >
          {home.hero.cta.label}
        </a>
      </div>
    </div>
  </section>

  <section class="photo-showcase reveal" aria-label="886 Studios photo slideshow">
    <div class="photo-rail" aria-label="886 Studios photo slideshow">
      <div class="photo-track">
        {photoLoopSets.map((isHidden) => (
          <div class="photo-track-set" aria-hidden={isHidden ? "true" : undefined}>
            {
              home.photos.items.map((photo) => (
                <figure class="photo-slide">
                  <img src={photo.src} alt={isHidden ? "" : photo.alt} decoding="async" />
                </figure>
              ))
            }
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Programs strip -->
  <div class="section reveal">
    <h2 class="section-h2">{home.programs.title}</h2>
    <div class="home-programs-flow">
      <article class="home-program-feature">
        <div class="home-program-feature-grid">
          <div>
            <h3 class="home-program-feature-title">{home.programs.featured.title}</h3>
            <p class="home-program-feature-copy">{home.programs.featured.body}</p>
          </div>
          <a class="card-cta home-program-feature-link" href={home.programs.featured.ctaHref}>
            {home.programs.featured.ctaLabel}
          </a>
        </div>
      </article>
      <div class="home-program-mini-grid">
        {
          home.programs.secondary.map((item) => (
            <article class="home-program-mini">
              <div class="home-program-mini-body">
                <h3 class="home-program-mini-title">{item.title}</h3>
                <p class="home-program-mini-copy">{item.body}</p>
              </div>
              <a class="card-cta" href={item.ctaHref}>
                {item.ctaLabel}
              </a>
            </article>
          ))
        }
      </div>
    </div>
  </div>

  <div class="section reveal reveal-delay-1">
    <h2 class="section-h2">{home.founders.title}</h2>
    <div class="logo-wall">
      {
        home.founders.logos.map((logo) => (
          <div class="logo-card">
            <div class="logo-stage">
              <img class:list={["logo-image", logo.imageClass]} src={logo.src} alt={logo.alt} />
            </div>
            <div class="logo-caption">{logo.name}</div>
          </div>
        ))
      }
    </div>
    <h3 class="portfolio-heading">Our portfolio</h3>
    <div class="portfolio-strip" aria-label="Portfolio companies">
      <div class="portfolio-marquee">
        <div class="portfolio-track">
          {home.founders.portfolio.map((item) => <span class="portfolio-item">{item}</span>)}
        </div>
        <div class="portfolio-track" aria-hidden="true">
          {home.founders.portfolio.map((item) => <span class="portfolio-item">{item}</span>)}
        </div>
      </div>
    </div>
  </div>

  <!-- Newsletter -->
  <div class="nl-strip">
    <div class="nl-inner">
      <div class="nl-text">
        <h3>{home.newsletter.title}</h3>
        <p>{home.newsletter.body}</p>
      </div>
      <form class="nl-form" action={home.newsletter.action} method="post" target="substack-subscribe-frame">
        <label class="sr-only" for="newsletter-email">Email address</label>
        <input
          id="newsletter-email"
          class="nl-input"
          type="email"
          name="email"
          placeholder={home.newsletter.placeholder}
          autocomplete="email"
          required
        />
        <button class="nl-btn" type="submit">{home.newsletter.button}</button>
      </form>
      <iframe class="nl-frame" name="substack-subscribe-frame" title="Newsletter signup status"></iframe>
    </div>
  </div>
</div>

<script>
  const newsletterForm = document.querySelector('.nl-form');

  newsletterForm?.addEventListener('submit', () => {
    const button = newsletterForm.querySelector('.nl-btn');
    newsletterForm.classList.add('is-submitted');

    if (button instanceof HTMLButtonElement) {
      button.textContent = 'Check your inbox';
    }
  });
</script>
