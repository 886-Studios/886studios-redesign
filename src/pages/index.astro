---
import HomePage from "../components/pages/HomePage.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { getPortfolioItemListSchema, getProgramSchema, pageMeta } from "../lib/seo";
---

<BaseLayout
  title={pageMeta.home.title}
  description={pageMeta.home.description}
  dateModified={pageMeta.home.dateModified}
  structuredData={[getProgramSchema(), getPortfolioItemListSchema()]}
>
  <HomePage />
</BaseLayout>
