{
  "name": "886studios-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "validate": "npm run check && npm run test && npm run build && npm run check:seo",
    "indexnow": "node scripts/submit-indexnow.mjs",
    "indexnow:dry-run": "node scripts/submit-indexnow.mjs --dry-run"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^7.0.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "fast-xml-parser": "^5.10.1",
    "sanitize-html": "^2.17.6"
  },
  "overrides": {
    "yaml": "2.8.3"
  }
}
